#!/bin/bash

unixpath=`winepath -u "$@"`
physicpath=`readlink -f "$unixpath"`
if [ -d "$physicpath" ]; 
then
	exec nautilus "$physicpath";
elif [ -f "$physicpath" ];
then
	exec gedit "$physicpath";
fi
